# --- Database (Postgres) ---
# `docker compose -f docker-compose.dev.yml up -d` starts a matching local Postgres.
DATABASE_URL="postgres://bookclub:bookclub@localhost:5432/bookclub"

# --- Auth.js ---
# Generate one with: npx auth secret  (or: openssl rand -base64 32)
AUTH_SECRET="dev-secret-change-me"
# Trust the local host in dev; set to your real URL in prod.
AUTH_URL="http://localhost:3000"

# --- Magic-link email delivery ---
# In development, leave RESEND_API_KEY unset: the magic-link URL is printed to the
# server console so you can sign in without any email provider.
# In production, set a Resend API key and a verified from-address.
# RESEND_API_KEY=""
EMAIL_FROM="Book Club <no-reply@bookclub.local>"

# --- Initial admin (npm run db:seed / runs automatically in Docker) ---
# The admin is the group owner and can invite others by sharing sign-up links.
# Created on first run; re-seeding re-syncs the password, so change it here to
# rotate it. Sign in with these at /login.
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="change-me-please"
ADMIN_NAME="Admin"

# --- Seed (optional) ---
SEED_GROUP_NAME="The Book Club"
# Extra members to pre-create (comma-separated). These have no password and would
# sign in via magic link; usually you just invite people with a link instead.
SEED_MEMBER_EMAILS=""
